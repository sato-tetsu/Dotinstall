  "use strict";

{
  const point = {
  	x: 100,
  	y: 180,
  };

  // const keys = Object.keys(point);
  // keys.forEach(key => console.log(`Key: ${key} ${point[key]}`));

  const points = [
    {x: 30, y: 20},
    {x: 40, y: 15},
    {x: 50, y: 10},
  ];
  console.log(points[1].y);
}